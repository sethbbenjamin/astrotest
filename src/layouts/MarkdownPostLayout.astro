---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.title}>
  <p>Written by the trickster: {frontmatter.author}</p>
  <p>Published: {frontmatter.pubDate.slice(0, 10)}</p>
  <slot />
</BaseLayout>
